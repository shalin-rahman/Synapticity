# Synapticity Framework: Project Task List
# Maintained per instruction: update tasks.txt with status BEFORE implementing each feature.
# Format: [x] = Completed | [ ] = Pending | [~] = In Progress

---

## Phase 1: Environment & Foundation
- [x] Initialize synaptic directory structure:
    - [x] `agents/` — Core Persona markdown files
    - [x] `skills/` — Technical Playbook markdown files
    - [x] `workspace/` — Per-mission isolated state storage
- [x] Create `.env.example` template with all required environment keys
- [x] Create professional `.gitignore` excluding secrets, caches, and virtual environments
- [x] Create `requirements.txt` pinning all production dependencies

---

## Phase 2: Agent Personas (agents/)
- [x] Create `orchestrator.md` — Master coordinator for multi-agent planning
- [x] Create `product-manager.md` — Translates goals into strict AC and data models
- [x] Create `software-engineer.md` — Writes SOLID-compliant, idiomatic source code
- [x] Create `tester.md` — QA agent verifying runtime outputs against specs
- [x] Create `oncall-engineer.md` — Security auditor using Bandit static analysis
- [x] Create `writer.md` — Technical documentation generator
- [x] Create `devops-engineer.md` — CI/CD pipeline synthesizer for GitHub Actions

---

## Phase 3: Core Engine (synaptic/)
- [x] Implement `GeminiAdapter`: Multi-key API rotation pool with per-key rate limiting
- [x] Implement `OllamaAdapter`: Local LLM execution engine (http://localhost:11434)
- [x] Implement `AgentRunner`: Persona-based prompt construction, model dispatch, MemoryLogger integration
- [x] Implement `SkillRegistry`: Keyword-triggered semantic playbook injector
- [x] Implement `MissionEngine`: Stateful 4-phase SDLC orchestrator

---

## Phase 4: Command Center (main.py)
- [x] Build Unified CLI: `dash`, `launch`, `stress`, `doctor`, `help`
- [x] Implement usage logger and live dashboard monitoring via `rich` tables
- [x] Add extended payload commands: `update`, `resume`, `remove`, `agent`, `audit`
- [x] Add `log` command to render `MISSION_LOG.md` in terminal via `rich.Markdown`
- [x] Add `deploy` command wired to `GitDeployer` for autonomous GitHub pushes
- [x] Add `ingest` command for polymorphic remote playbook ingestion
- [x] Add `admin` command with secure `getpass` passcode gateway
- [x] Add `review` command for external project indexing and multi-agent code review

---

## Phase 5: Skills (skills/)
- [x] Create `fastapi-expert` playbook — REST API standards and async patterns
- [x] Create `security-scanner` playbook — OWASP, secrets detection, input validation
- [x] Create `context-filter` playbook — Relevance filtering for large context windows
- [x] Create `code-quality-linting` playbook — Always-injected baseline quality standards

---

## Phase 6: Validation & Integration Testing
- [x] Updated `stress_test.py` for synaptic Gemini API pool performance benchmarking
- [x] Execute Mission 001 (`mission-vault` — Encrypted FastAPI vault, completed successfully)
- [x] Validate synaptic Cache persistence (`specs.json` hash-based retrieval confirmed functional)
- [x] Execute smoke test suite (`scratch/smoke_test.py`) — 20/20 modules validated, 0 failures

---

## Phase 7: Interactive Gating & Checkpointing (v3.0)
- [x] Implement `state.json` persistence enabling mission pause/resume across sessions
- [x] Add `INTERACTIVE_MODE` flag for Human-In-The-Loop (HITL) milestone approvals
- [x] Implement Milestone Gating: PM Approval -> SWE Code Review -> Final Go-Live
- [x] Create seamless State Machine phase coordination (START -> PLANNED -> DEVELOPED -> VERIFIED -> COMPLETED)
- [x] Extract `ApprovalGate` into dedicated `synaptic/core/mission_gate.py` (SOLID refactor)

---

## Phase 8: Hardened Runtime Testing
- [x] Implement `RuntimeRunner` for safe local Python execution via temp files
- [x] Capture `stdout`/`stderr` and real runtime logs injected into Tester agent context
- [x] Implement `scan_security` — Bandit static vulnerability analysis integration
- [x] Implement `scan_quality` — `ruff`/`pylint` IDE Problems window simulation
- [x] Add Docker containerization: `USE_DOCKER_SANDBOX=True` routes execution to `python:3.11-slim` container
- [x] Config-driven toggle: `USE_DOCKER_SANDBOX` (default: `False`, bare-metal execution)
- [x] Implement Admin Role CLI passcode gate (Default: `alpha-tango-77`, configurable via `.env` `ADMIN_PASSCODE`)

---

## Phase 9: Professional Hand-off
- [x] Create `writer.md` Documentation Agent persona
- [x] Auto-generate `TECHNICAL_DOCS.md` in `output/` after each successful mission
- [x] Implement structured JSON execution logs (state history) for mission auditing via `audit` command

---

## Phase 10: Extensibility & Automated Skill Sourcing
- [x] Architect abstract `RemoteResourceFetcher` base class for polymorphic ingestion
- [x] Build concrete `SkillIngestor` and `AgentIngestor` subclasses
- [x] Build unified `ingest <type> <url> <name>` CLI command (DRY: single registry dict routes to correct ingestor)
- [x] Implement SkillRegistry filesystem hot-reload: `mtime`-based diff detection on every `inject()` call
- [x] Config-gated: `ENABLE_HOT_RELOAD=True` (default), togglable without restart

---

## Phase 11: Telemetry, Logs & Failover Configuration
- [x] Implement background heartbeat thread (interval: `HEARTBEAT_INTERVAL` seconds) to keep CLI alive during long Ollama calls
- [x] Construct `MemoryLogger` — lossless JSONL capture of all system prompts, user prompts, and LLM outputs
- [x] Implement hard `INACTIVE` config switches: `GEMINI_ACTIVE`, `OLLAMA_ACTIVE` (both in `.env`)
- [x] Dynamic model routing in `MissionEngine.__init__`: Ollama-first, Gemini fallback, fatal halt if both inactive
- [x] Replace all emoji output with strict professional text-bracket tags (`[OK]`, `[FAIL]`, `[REPAIR]`, `[DEVOPS]`)

---

## Phase 12: Autonomous Deployment & CI/CD Pipelines
- [x] Create `devops-engineer.md` persona for tech-stack-aware GitHub Actions YAML synthesis
- [x] Integrated into `MissionEngine` Phase 4 — pipeline auto-generated after code is verified
- [x] Pipeline saved to `output/.github/workflows/main.yml` — ready for direct GitHub push
- [x] Implement `GitDeployer` (`synaptic/utils/git_deployer.py`): `git init`, GitHub REST API repo creation, `git push`
- [x] Wire `./sync deploy <mission_id>` CLI command using `GITHUB_USER` and `GITHUB_TOKEN` from `.env`

---

## Phase 13: Meta-Cognition & Architectural Grounding
- [x] Rewrite `architecture_map.md` to document: Infinite Model Pluggability, CI/CD Phase 4, Meta-Cognition guardrails
- [x] Update `README.md` with `deploy`, `ingest`, `review`, `admin`, `log` command documentation
- [x] Full emoji purge across `mission_engine.py`, `doctor.py`, `stress_test.py`, `logger.py`, `gemini.py`, `ollama.py`
- [x] Add `ADMIN_PASSCODE` and `USE_DOCKER_SANDBOX` to `config.py` and `.env.example`

---

## Phase 14: Mission Observability
- [x] Implement `./sync log <mission_id>` — renders `MISSION_LOG.md` via `rich.Markdown` in terminal
- [x] `MISSION_LOG.md` includes: objective, phase, verdict, full agent history with timestamps and truncated outputs

---

## Phase 15: SOLID CLI Refactoring
- [x] Extract every CLI action into a named single-responsibility `handle_*()` function
- [x] Shared helpers (`sanitize_id`, `require_args`, `load_state`, `save_state`) extracted to `synaptic/cli/utils.py`
- [x] UI rendering (`show_dashboard`, `show_help`) extracted to `synaptic/cli/ui.py`
- [x] Handler modules by domain: `handlers/mission.py`, `handlers/observe.py`, `handlers/integration.py`, `handlers/review.py`
- [x] `COMMAND_REGISTRY` dict in `synaptic/cli/registry.py` — OCP extension point (add new command = 1 file + 1 dict entry)
- [x] `main.py` reduced to a 33-line pure entrypoint (platform bootstrap, registry lookup, error boundary)

---

## Phase 16: SOLID/DRY Mission Engine Refactoring
- [x] Extract `MissionStateManager` (`core/mission_state.py`) — owns all `state.json` disk I/O
- [x] Extract `MissionLogger` (`core/mission_logger.py`) — owns history appending and `MISSION_LOG.md` generation
- [x] Extract `MissionWorkspace` (`core/mission_workspace.py`) — owns `output/` code, docs, and CI/CD YAML writes
- [x] Extract `ApprovalGate` (`core/mission_gate.py`) — owns HITL prompts; INTERACTIVE_MODE no-op when False
- [x] Extract `PlanningPhase` and `HealingCyclePhase` (`core/mission_phases.py`) — phase logic isolated from orchestration
- [x] Extract `resolve_runner()` (`core/agent_dispatcher.py`) — DRY alias resolution map for all agent names
- [x] Rewrite `MissionEngine` as thin ~120-line orchestrator delegating to all sub-systems
- [x] `MissionEngine` now passes isolated unit testing per sub-system independently

---

## Phase 17: External Project Intelligence
- [x] Implement `ProjectIndexer` (`utils/project_indexer.py`): recursive directory crawler with skip-list, 50KB file cap, multi-extension support
- [x] Implement `ProjectReviewEngine` (`core/project_review_engine.py`): 3-agent review pipeline (PM -> SWE -> Security)
- [x] Saves `REVIEW_REPORT.md` into mission workspace for permanent record
- [x] Implement `FILE_PATCH` directive parser: agent can emit structured patches applied back to target files
- [x] Wire `./sync review <id> <path> "<goal>" [--apply]` CLI command (read-only by default, `--apply` enables write-back)
- [x] Registered in `COMMAND_REGISTRY` and help panel

## Phase 18: Performance & Async Optimization
- [x] Implement **Parallel Verification**: QA and Security agents now dispatch simultaneously in the Healing Cycle
- [ ] Refactor Model Adapters for `asyncio` compatibility to eliminate I/O blocking during long LLM calls
- [x] Implement Turbo-Remediation: Automated parser for common technical syntax/markdown cleanup
- [ ] Persistent Resource Cache: Index common library patterns to reduce PM/SWE reasoning overhead

## Phase 19: Autonomous Self-Learning & Meta-Cognition
- [x] Create `ReflectorAgent` persona for post-mission critical analysis
- [x] Implement `SelfLearningEngine` to convert mission logs into permanent skill playbooks
- [x] Implement `./sync learn <mission_id>` command to trigger reflection cycles
- [x] Auto-inject `skills/autonomous-lessons.md` into every new mission context for continuous improvement

## Phase 20: Role Job Performance Analytics
- [x] Implement Agent Performance Scorecards (Success/Fix/Failure metrics per persona)
- [x] Implement `synaptic/core/analytics.py`: Track tokens, latency, and "Stress Level" per role (v3.0)
- [x] Implement `./sync stats`: Visual dashboard of agent reliability and model health
- [x] Autonomous Role Rotation: Dynamically promote fallbacks if primary scores drop below 40%

---

## [PENDING] Future Enhancements
- [ ] Unit test suite: mock-based tests for each `MissionEngine` sub-system independently
- [ ] Multi-file output support: SWE agent emits multiple `FILE_PATCH` blocks for complex projects
- [ ] Web UI dashboard: `rich` + `textual` TUI or lightweight Flask/FastAPI status page
- [ ] Auto-pull Ollama model if missing (extend `SynapticDoctor.heal()`)
- [ ] Plugin system: drop any `synaptic/plugins/<name>.py` and auto-discover at startup
